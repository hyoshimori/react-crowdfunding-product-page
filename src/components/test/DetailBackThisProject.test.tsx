import React from 'react';
import { render } from '@testing-library/react'
import DetailBackThisProject from '../main/DetailBackThisProject';

describe("# DetailBackThisProject component", () => {
  test('if DetailBackThisProject component exsits or not', () => {
    render(<DetailBackThisProject />)
  })
})
