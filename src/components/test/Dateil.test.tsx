import React from 'react';
import { render } from '@testing-library/react';
import Detail from '../main/Detail';

describe('# Detail component', () => {
  test('if Detail component exists or not', () => {
    render(<Detail />)
  })
})
